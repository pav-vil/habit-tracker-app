# Example environment variables - Copy to .env and update values
# NEVER commit the actual .env file!

# Flask environment (development, production, testing)
FLASK_ENV=development

# Secret key for session management (generate with: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=change-this-to-a-random-64-character-secret-key

# Database URL
# Development (SQLite): sqlite:///habits.db
# Production (PostgreSQL): postgresql://username:password@host:5432/database_name
DATABASE_URL=sqlite:///habits.db

# Redis URL for rate limiting (optional, defaults to in-memory)
# REDIS_URL=redis://localhost:6379/0
