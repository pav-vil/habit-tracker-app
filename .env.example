# Example environment variables - Copy to .env for local development
# NEVER commit the actual .env file!

# ==========================================
# FLASK ENVIRONMENT
# ==========================================
# Options: development, production, testing
FLASK_ENV=development

# ==========================================
# SECRET KEY (REQUIRED FOR PRODUCTION)
# ==========================================
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# For Render.com: Set this in Environment Variables dashboard
SECRET_KEY=change-this-to-a-random-64-character-secret-key

# ==========================================
# DATABASE URL
# ==========================================
# Development (SQLite): sqlite:///habits.db
# Production (PostgreSQL on Render): Auto-populated as DATABASE_URL
# Note: Render provides this automatically when you add a PostgreSQL database
DATABASE_URL=sqlite:///habits.db

# ==========================================
# REDIS URL (Optional - for rate limiting)
# ==========================================
# Local: redis://localhost:6379/0
# Render: Use Render Redis add-on, auto-populated as REDIS_URL
# If not set, falls back to in-memory storage
# REDIS_URL=redis://localhost:6379/0

# ==========================================
# STRIPE PAYMENT CONFIGURATION (Phase 3)
# ==========================================
# Get these from: https://dashboard.stripe.com/apikeys
# Use test keys (sk_test_xxx) for development, live keys (sk_live_xxx) for production

# Stripe API Keys
STRIPE_SECRET_KEY=sk_test_your_test_secret_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_test_publishable_key_here

# Stripe Webhook Secret (from Stripe Dashboard > Developers > Webhooks)
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Stripe Price IDs (create products in Stripe Dashboard first)
# Monthly Subscription: $2.99/month
STRIPE_PRICE_ID_MONTHLY=price_xxxxxxxxxxxxx

# Annual Subscription: $19.99/year (BEST VALUE)
STRIPE_PRICE_ID_ANNUAL=price_xxxxxxxxxxxxx

# Lifetime Purchase: $59.99 one-time
STRIPE_PRICE_ID_LIFETIME=price_xxxxxxxxxxxxx

# ==========================================
# APP URL (for payment redirects)
# ==========================================
# Development: http://localhost:5000
# Production: https://your-domain.com
APP_URL=http://localhost:5000

# ==========================================
# PAYPAL PAYMENT CONFIGURATION (Phase 5)
# ==========================================
# Get these from: https://developer.paypal.com/dashboard/
# Use sandbox credentials for development, live credentials for production
#
# Setup Steps:
# 1. Create PayPal Business account (or use sandbox account)
# 2. Go to https://developer.paypal.com/dashboard/applications/sandbox
# 3. Create a REST API app to get Client ID and Secret
# 4. Create subscription plans in PayPal Dashboard
# 5. Get plan IDs from the subscription plans

# PayPal Mode (sandbox for development, live for production)
PAYPAL_MODE=sandbox

# PayPal API Credentials
PAYPAL_CLIENT_ID=your_paypal_client_id_here
PAYPAL_CLIENT_SECRET=your_paypal_client_secret_here

# PayPal Webhook ID (for signature verification)
# Get this after setting up webhooks at https://developer.paypal.com/dashboard/webhooks
PAYPAL_WEBHOOK_ID=your_webhook_id_here

# PayPal Subscription Plan IDs (create these in PayPal Dashboard first)
# Monthly Plan: $2.99/month
PAYPAL_PLAN_ID_MONTHLY=P-xxxxxxxxxxxxx

# Annual Plan: $19.99/year
PAYPAL_PLAN_ID_ANNUAL=P-xxxxxxxxxxxxx

# Note: PayPal doesn't support one-time "lifetime" subscriptions in the same way
# Stripe does. For lifetime access, use Stripe or Coinbase Commerce.

# ==========================================
# COINBASE COMMERCE CONFIGURATION (Phase 6)
# ==========================================
# Get these from: https://commerce.coinbase.com/
# Coinbase Commerce allows Bitcoin/cryptocurrency payments for lifetime tier
#
# Setup Steps:
# 1. Create Coinbase Commerce account at https://commerce.coinbase.com/
# 2. Go to Settings > API Keys
# 3. Create a new API key
# 4. Set up webhook endpoint at Settings > Webhook subscriptions
#    - Webhook URL: https://your-domain.com/webhooks/coinbase
#    - Select events: charge:confirmed, charge:failed, charge:pending
# 5. Copy the Webhook Shared Secret
#
# Note: Coinbase Commerce is for ONE-TIME payments only (perfect for lifetime tier)
# NOT for subscriptions - use Stripe/PayPal for recurring subscriptions

# Coinbase Commerce API Key
COINBASE_COMMERCE_API_KEY=your_coinbase_commerce_api_key_here

# Coinbase Commerce Webhook Secret (for signature verification)
COINBASE_COMMERCE_WEBHOOK_SECRET=your_webhook_shared_secret_here

# Lifetime Tier Price (in USD - will be converted to crypto at checkout)
COINBASE_LIFETIME_PRICE=59.99

# ==========================================
# EMAIL CONFIGURATION (Flask-Mail)
# ==========================================
# For Gmail (Development only):
# 1. Enable 2FA on your Google account
# 2. Generate App Password: https://myaccount.google.com/apppasswords
# 3. Use the 16-character app password below (not your regular password)
#
# For production (recommended):
# - SendGrid: MAIL_SERVER=smtp.sendgrid.net, MAIL_USERNAME=apikey
# - Mailgun: MAIL_SERVER=smtp.mailgun.org

MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USE_SSL=False
MAIL_USERNAME=your.email@gmail.com
MAIL_PASSWORD=your_app_password_here
MAIL_DEFAULT_SENDER=HabitFlow <noreply@habitflow.app>
MAIL_MAX_EMAILS=100

# ==========================================
# RENDER.COM DEPLOYMENT NOTES
# ==========================================
# 1. Set FLASK_ENV=production in Render dashboard
# 2. Generate strong SECRET_KEY and add to Render environment variables
# 3. Add PostgreSQL database - DATABASE_URL auto-populated
# 4. (Optional) Add Redis instance for production rate limiting
# 5. Add all STRIPE_* variables from your Stripe dashboard (use live keys)
# 6. Add all MAIL_* variables for email provider (SendGrid/Mailgun)
